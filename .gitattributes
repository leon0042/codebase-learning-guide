# 统一换行符：文本文件一律 LF，避免跨平台协作时整文件 diff
* text=auto eol=lf

# 显式声明文本类型
*.md   text eol=lf
*.py   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# 二进制文件不做转换
*.png  binary
*.jpg  binary
*.pdf  binary
