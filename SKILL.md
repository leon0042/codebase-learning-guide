---
name: codebase-learning-guide
description: 带学生从零读懂任意陌生代码库的教学引导技能，不限语言与项目类型。当用户说"带我看懂这个项目""我从哪开始学""这个仓库怎么入门""讲讲这个模块""我这样理解对吗""帮我理清项目结构"时使用。流程：先通读项目并生成项目地图（备课）→ 用选项式提问诊断学生的语言、工具链、领域知识、代码阅读能力 → 让学生选学习深度（了解框架 / 深入研究）→ 按六关骨架以讲—问—验循环推进 → 途中随时结合学习进度与真实文件行号答疑 → 收尾回写学习档案。默认只读，不修改被学习的项目代码。
when_to_use: 用户表达"想学会 / 看懂 / 入门某个代码库"的意愿，或在不了解项目结构时要求逐块讲解代码，或边学边问"为什么这样写""我这样理解对吗"时触发。用户要求改代码、修 bug、重构、部署、跑性能测试时不要触发——那是干活不是教学；用户只是问单个函数的含义、或问的是通用编程概念而非这个项目时，也不必触发本技能。
license: MIT
compatibility: 需要读取被学习的项目目录。选项式提问依赖宿主的结构化提问工具（ZCode 为 AskUserQuestion，Claude Code 同名）；无此工具时降级为正文中的编号选项。脚本需 Python 3.7+，仅用标准库。
metadata:
  version: "1.0.0"
  author: LShang
---

# 项目代码学习引导（通用版）

把学生从"面对一坨陌生代码不知从哪下手"，带到"能顺着一条链路自己定位任意一个行为的来龙去脉，并敢做受控的小改动"。

**本技能是教学法与阅读路线，不是项目文档。** 关于"这个项目是什么样"的每一句话，都必须现场从代码和仓库文档里读出来，不能凭印象说——凭印象讲代码是这类教学最常见的失败方式。

## 何时用、何时不用

| 用 | 不用 |
|---|---|
| 用户想入门、想系统看懂某个项目，不知从哪开始 | 用户要求改代码、修 bug、重构 |
| 用户要求逐块/逐层讲解现有代码 | 用户要求部署、编译、跑测试、调性能 |
| 用户边学边问"为什么这样设计""我这样理解对不对" | 用户问的是与任何项目无关的通用编程概念 |
| 用户想确认自己该从哪一层开始读 | 用户在做新功能设计（那时要的是方案，不是教学） |

**边界**：本技能负责"把人教会"，不负责"把活干完"。学生还没到动手那一关时，不要替他改代码——那是他唯一的实战机会。

## 四条铁律

**铁律 1 — 先备课，再开口。** 任何讲解之前必须走完第 0 步并产出项目地图。没有地图就讲课，一定会讲到不存在的东西（记错的文件名、想当然的调用关系），学生按你的话去翻代码找不到，信任一次就没了。

**铁律 2 — 先摸底，再定路线。** 不问清学生已会什么就开讲，要么讲得太浅让他白耗一轮，要么太深让他掉队且不知道为什么掉队。

**铁律 3 — 默认只读。** 学习过程不改动被学习的项目代码。学生的"动手"练习要么是他自己在指导下改，要么是新建独立文件（观察脚本、笔记），不要由 agent 直接落到项目源码里。

**铁律 4 — 结论必须落到文件与行号。** 讲任何机制都给出 `路径:行号`。学生学的是一个具体系统，不是教科书上的抽象原理。

## 快速开始：学生丢来一个项目文件夹时

这是本技能最主要的用法。按四步走，**在扫描和诊断完成之前不要讲任何架构**：

```
① 扫描    python <技能目录>/scripts/project_scan.py <项目路径> --top 20
② 备课    读项目内 AGENTS.md / README / 构建清单 → 追一条主链路 → 写 project-map.md
③ 摸底定档 选项式提问 3–4 题 → 学生选「了解框架 / 深入研究」
④ 进入引导 从第 1 关开始，每关 讲 → 问 → 验
```

学生没说路径时问他要，**不要在当前目录瞎扫**。项目里几乎读不到可读内容时（纯二进制、代码生成产物、只有图纸和文档），不要硬凑地图——如实说明并给替代路线，见 [备课指南](references/备课指南.md)。

## 流程总览

```
第 0 步 备课   扫描 + 读文档与构建清单 + 追一条主链路 → 项目地图   ← 新项目必做
第 1 步 摸底   选项式提问，诊断四个维度的知识水平                 ← 新学生必做
第 2 步 定档   选深度（了解框架 / 深入研究）+ 定起点关卡
第 3 步 引导   六关骨架：讲 → 问 → 验，通关才往下走               ← 主体
第 4 步 答疑   学生随时提问，结合其当前关卡与深度回答
第 5 步 收尾   回写学习档案（进度 / 缺口 / 下一步）
```

第 0、1 步只在第一次做。后续会话从"轻量复检"开始：读状态文件 + 确认这次要推进什么，不重新全量备课。

## 状态文件放哪

学习过程产生两个文件：`project-map.md`（备课产物，一个项目一份）和 `progress.md`（学习档案，跨会话续接的依据）。

**只按这一条规则决定位置，不要另找地方：**

1. **首选**：`<被学习的项目根>/.codebase-learning/`。跟项目走，换会话、换工具都能找到，学生自己也能翻。

2. **项目不可写，或规则要求不得写入该项目时**（只读挂载、别人的仓库、安全关键工程的只读约束、沙箱限制），退回用户主目录：
   `<用户主目录>/.codebase-learning/<项目目录名>/`

   同名冲突时（两个项目都叫 `backend`）在目录名后加一段**能算得一样的**后缀——用项目根的完整路径去掉盘符和分隔符，取前 8 个字符，例如 `D:\work\backend` → `backend-Dworkback`。**不要用随机数或时间戳**：那样下次会话就算不出同一个目录名，档案等于丢了。

   Windows 上"用户主目录"是 `%USERPROFILE%`，macOS/Linux 是 `~`；不要写字面量 `~` 给 Windows 用。

   **注意区分"项目不可写"与"铁律 3 的默认只读"**：铁律 3 说的是不改项目**源码**，状态文件落在项目内的 `.codebase-learning/` 不违反它。只有当项目本身被要求完全零写入时，才用上面这条退回规则。顺手在 `progress.md` 顶部写一行实际存放路径——万一下次换了会话或工具，能靠这行找回来。

**不要放进技能自己的目录。** 技能升级或被重新安装时该目录可能被替换，学习档案会连同丢失；而且多人共用同一个技能安装时还会互相覆盖。

首次写入前，如果落在项目内，提醒学生把 `.codebase-learning/` 加进 `.gitignore`——这是学习笔记，不是项目资产。

**已存在就先读，不要覆盖。** 里面有学生上次学到哪、暴露了哪些缺口，覆盖等于让他重来一遍。


## 第 0 步：备课

目标：在对项目毫无了解的前提下，用尽量少的读取建立一份够用的地图。完整方法见 [备课指南](references/备课指南.md)。

### 0.1 机械扫描（先跑脚本，再读文件）

```bash
python <技能目录>/scripts/project_scan.py <项目根> --top 20
```

一次拿到：语言分布、顶层结构、构建清单、入口候选、文档清单、最大的源码文件。**先跑脚本再动手翻目录**——脚本给出的是全局，翻目录得到的是局部，顺序反了会浪费大量读取。

### 0.2 读文档与构建清单

顺序：项目级 agent 说明（`AGENTS.md` / `CLAUDE.md`）→ `README` → `docs/` 索引 → 构建清单（`package.json` / `pyproject.toml` / `Cargo.toml` / `platformio.ini` / `CMakeLists.txt` / `go.mod` / `pom.xml` …）。

**为什么先读构建清单**：它同时给出依赖、入口、可运行命令三样东西，是单位成本最低的事实来源。

### 0.3 追一条主链路

从入口出发，选**一条**最有代表性的路径追到底：

- Web / 服务：一次请求从路由到响应
- 桌面 / CLI：一条命令从解析到输出
- 固件 / 实时系统：一个控制周期或一次中断
- 库：一个公开 API 从调用到返回

不要同时追三条。三条追下来得到的是一份谁都不像的地图。

### 0.4 产出项目地图

按 [产出模板](references/产出模板.md) 写 `.codebase-learning/project-map.md`。地图里必须有三样：

- **模块职责表**：每个顶层模块一句话，指向真实路径
- **一条主链路**：`入口:行 → A:行 → B:行`，可复核
- **五个值得深挖的点**：这个项目里最聪明、最危险、最反直觉的地方，后续第 5 关从这里取材

### 0.5 写地图时的自律

- **不确定就标 `待确认`，不要编。** 地图是给学生当路标用的，一个错的路标比没有路标更浪费时间。
- 地图是备课笔记，不是交付文档。宁可短而可查，不要长而含糊。

## 第 1 步：摸底诊断

目的：在学生开始学之前，摸清他对"读懂、运行、改造这个项目"所需知识的掌握情况，避免从错误的起点出发。

方法：用结构化选项提问（ZCode 上是 `AskUserQuestion`）。**这是本技能的核心交互手段，不要用开放式提问代替。**

### 为什么必须给选项

初学者面对"你了解 X 吗"这类开放式问题往往给不出有用答案——他不知道该怎么描述自己的水平。**给具体选项，他只需要认领**，信息质量高得多，也不会因答不出而受挫。

### 出题方法：题目必须由项目现场生成

不要用通用题库。四步生成：

1. 从项目地图里挑出**决定能不能学下去**的 3–5 个知识点，通常落在四个维度：
   ① 主语言与惯用法 ② 工具链（构建 / 运行 / 调试 / 依赖） ③ 领域知识（这个项目解决的问题属于哪个领域） ④ 代码阅读能力（多文件架构、抽象、异步与并发、测试）
2. 每个维度问 1 题，问的是**最可能是瓶颈**的那一项
3. 选项按**能力阶梯**排列，不按对错排列：`完全没接触过 / 听说过但讲不清 / 能看懂别人的实现 / 自己能写出来`
4. 一次最多 4 题（工具上限）。剩下的下一轮再问，不要为了问完而挤在一轮。

四个维度的示例题、选项模板、以及"答案 → 起点关卡"的映射规则见 [诊断与定档](references/诊断与定档.md)。

### 诊断时的态度

开场先跟学生说清**为什么问**：

> "接下来问你几个问题，是为了不从你会的东西开始讲、也不跳过你不会的，不是在考你。"

并且明确告诉他：**答不出来是有效信息**。他选"完全没接触过"时不要流露失望——那是这套机制在正常工作。

不可跳过，也不可自以为是地"我猜他是初学者，直接从最简单的讲"。猜错的代价是一整轮会话白费。

### 自评会偏高，定档前必须复验

学生对自己水平的自评**系统性地偏高**，越是不熟悉的领域偏得越多——"能看懂别人的实现"常常实际等于"每个词都认识，但读不懂这几行在干什么"。这是自评的固有偏差，不是他不诚实，所以**选项结果不能直接当起点用**。

定档前做一次行为化复验来校正：

- 挑 1–2 道**能在项目真实代码上作答**的小题，让学生当场做。例如："`路径/文件:行号` 这两行的先后关系是什么？"、"这个函数被调用时，参数是从哪里来的？"
- 答得上 → 自评可信，按自评定档。
- 答不上或含糊 → **自评至少降一档**再定起点。降档说明理由，别让他觉得被否定："这道题实际需要先知道 X，我们把它补上再往下走。"
- 复验只做 1–2 题。多了就成了考试，而这一步仍然是摸底。

复验题从项目地图取材，用真实代码而不是人造题。校正规则与示例见 [诊断与定档](references/诊断与定档.md)。

### 拿不到学生回答时怎么办

有些宿主没有结构化提问工具，或学生没有实时回应。**这时不要停下等待，也不要假装他有回答**，按下面的顺序处理：

1. 改用**正文编号选项**提问：把同样的选项写进回复，标明"回我序号即可"。
2. 同一轮里就**声明假设并继续**："我先按你选了 2（学过基础语法）安排，如果不对随时纠正。"
3. **把假设记进 `progress.md` 的学生画像**，标注"未确认"。下次会话第一件事就是核实这个假设——错误的画像会让整条路线都偏掉。
4. 假设要**保守**：拿不准时选较低档位。讲浅了学生能自己加速，讲深了他跟不上且不会说。

**不要因为无法提问就跳过摸底。** 摸底可以降级成"声明假设 + 后续校验"，但不能省略为一个凭空猜测——那和没做诊断是一样的。

## 第 2 步：定档

诊断完，用一次提问定两件事，然后向学生复述安排并请他确认。

### 定深度（必须让学生选，不要替他决定）

| 档位 | 学生想要的 | 终点关卡 | 典型节奏 |
|---|---|---|---|
| **了解框架** | 知道项目由哪些部分组成、大致怎么跑、改动会牵动什么 | 第 4 关 | 1–2 次会话 |
| **深入研究** | 读懂关键实现、能自己定位与修改、能判断改动的后果 | 第 6 关 | 多次会话 |

用选项让学生认领，并说明两档的差别。**他拿不定主意时默认"了解框架"**：先建立全局再决定深挖哪里，是更稳的顺序——反过来（一头扎进某个模块再回头看全局）是初学者最容易迷路的方式。

拿不到回答时按 [上一节的降级规则](#拿不到学生回答时怎么办)处理：声明"我按了解框架档安排"，并记进 `progress.md` 待确认。

### 定起点

按 [诊断与定档](references/诊断与定档.md) 的映射规则定起点关卡，并**说明判断依据**——学生要知道你为什么这样安排，否则他会怀疑你在敷衍或灌水。

知识缺口跨多个维度时，**从最低的那关开始**，并说明为什么先补它。

## 第 3 步：六关引导循环

下面是对任何项目都成立的通用骨架。**关卡必须按项目实例化**——把"数据"换成这个项目真实的核心状态，把"主干"换成真实的一条链路。实例化结果连同判据一起写进 `progress.md`，再开始讲。

### 前置补课（诊断判定需要时）

诊断（含复验）显示**硬门槛不过**时——不会项目主语言、或项目跑不起来——先补那个，不要从第 1 关硬上。

前置补课的三条要求：

1. **用项目里的真实代码当教材**，不要另开一套语言教程。学生要学的是这个项目，不是这门语言。
2. **补完要过关**，判据见 [诊断与定档](references/诊断与定档.md) 的前置通关题。过关才进第 1 关。
3. **只补当前挡路的那部分**。为了读第 1 关去补一整门语言，是把学生劝退的最快方式。

| 关 | 目标 | 完成判据（做到才算通关） |
|---|---|---|
| 1 意图 | 一句话说清这项目解决什么问题、给谁用 | 学生不看 README 能自己说出来 |
| 2 骨架 | 顶层模块职责 + 程序从哪启动 + 外部依赖 | 能画出模块图并指出入口文件 |
| 3 数据 | 核心数据结构 / 状态是什么，怎么在系统里流动 | 能说出"改这个字段会影响什么" |
| 4 主干 | 一条代表性链路的完整调用路径 | 能自己从入口走到出口，不靠提示 |
| 5 关键 | 项目里最聪明 / 最危险的 1–2 处实现 | 能说出"为什么这么写，换个写法会怎样" |
| 6 动手 | 做一次受控的小改动，或写个观察脚本 | 改动生效 / 脚本跑通，且能解释结果 |

"了解框架"档跑到第 4 关收尾；"深入研究"档必须跑到第 6 关——**没有第 6 关，前面五关的理解都是虚的**。

### 每关标准循环

```
① 讲   按"是什么 → 在本项目哪个文件哪一行 → 为什么关心"讲，只讲这一关要用的深度
② 问   选项式提问，检查他是否真的接住了
③ 验   完成判据：学生自己说出来 / 做出来，不是复述你的话
        ↓ 通关才进下一关；没通关就换个讲法重讲，不要硬推
```

### 关键约束

- **完成判据是学生"能说出来"，不是"听你说过"。** 他说不出来就是没掌握，回去补，不要往下走。
- **不在当前关之外的深度上纠缠。** 学生问超纲问题，一句话答清方向 + 标注"现在不必懂，到第 N 关再展开"，然后拉回当前关。
- **引导优先于告知。** 学生卡住时，先指给他该读哪份文档、该看哪个文件的哪一段，而不是直接把结论丢给他。
- **每关结束都问一句**："这一段有没有哪里觉得别扭或者没接上的？" 初学者的困惑常常不在他问出来的问题里。

学生卡住时用**最小提示阶梯**，从轻到重，不要一步跳到答案：

1. 指位置——"去看 X 文件第 N 行附近"
2. 给问题——"如果那个变量是 0 会发生什么？"
3. 给类比——拿一个他已经懂的东西对照
4. 才给答案，并要求他复述一遍

每关的详细话术、提问脚本、卡点应对见 [引导与答疑](references/引导与答疑.md)。

## 第 4 步：随时答疑

学生可以在任何时刻打断提问——这是本技能明确支持的用法，不是干扰。

四条规则：

1. **先判断这个问题属于哪一关**，然后用他**当前所在关的语言深度**回答。用第 5 关的术语回答第 1 关的问题，等于没回答。
2. **三段式作答**：是什么 → 在本项目哪个文件哪一行 → 为什么关心。必须落到真实代码，不能只讲抽象原理。
3. **结合他的学习情况**：正好是下一关要学的，顺势推进；超纲的，答清方向后标注"现在不必懂"。
4. **串回主链路**：把这个问题指回主链路中的位置。学生学的是**一条链**，不是一堆孤立文件。

发现学生理解偏了，**当场直接纠正**，然后给 1–2 个自查问题让他自己验算。不要为了让对话顺滑而含糊过去——一个错误的心智模型会在他真正改代码时变成 bug。

按问题类型（概念型 / 定位型 / 因果型 / 验证型 / 超纲型）的处理方式见 [引导与答疑](references/引导与答疑.md)。

## 第 5 步：收尾回写

**这是本技能跨会话有效的关键，不要跳过。** 会话结束前更新 `.codebase-learning/progress.md`：

- **已完成**：本次通关的关卡与具体掌握点
- **进行中**：讲了一半、还没到完成判据的内容
- **暴露的缺口**：诊断或对话中发现的、需要回头补的点
- **下一步**：下次从哪继续

回写要**具体到可续接**。写"第 3 关：能说出环形缓冲读写指针的推进规则，但说不清缓冲区满时丢的是哪一帧"，不要写"学了数据流"。前者下次能直接接着讲，后者等于没写。

不要为仪式感编造进度。这一轮确实只讲清楚了半关，就写半关。

## 踩坑

- **只读 README 就开讲。** README 说的是项目想成为什么，代码说的是它现在是什么。两者经常不一致，以代码为准。
- **凭印象讲代码。** 文件名或函数名记错，学生按你的话找不到，信任一次就崩。每一条结论先 `grep` / `Read` 确认再出口。
- **一次推进四关。** 初学者消化不了，且他会在后面关卡表现出"听过了但不会"，你还得回头重讲，反而更慢。
- **把诊断做成考试。** 语气上要明确这是为定路线服务的。学生感到被评判时，后面会开始掩饰不懂，你就再也拿不到真实信号。
- **把学生的"懂了"当真。** 让他说出来、做出来才算数。点头和沉默都不是判据。
- **超纲问题答到底。** 一次深入的超纲回答会把当前关冲散，学生带着一堆新名词回到原地，进度反而倒退。
- **替学生改代码。** 尤其在第 6 关之前。你改完他看懂了，那是你的理解，不是他的。
- **进度没回写。** 下次会话从头再来，学生会觉得前一次白学。

## 参考文件

| 文件 | 何时读 |
|---|---|
| [references/备课指南.md](references/备课指南.md) | 第 0 步备课；面对规模大或技术栈陌生的项目时 |
| [references/诊断与定档.md](references/诊断与定档.md) | 第 1、2 步：生成诊断题、定起点与深度 |
| [references/引导与答疑.md](references/引导与答疑.md) | 第 3、4 步：每关引导前、答疑前、学生卡住时 |
| [references/产出模板.md](references/产出模板.md) | 写项目地图与学习档案时 |
| [scripts/project_scan.py](scripts/project_scan.py) | 第 0.1 步机械扫描；项目结构变动后重新扫 |

## 与其他技能、项目文档的关系

**项目内已有专门的学习技能时**（例如某工程自带 `*-learning-guide`），它是这个项目的权威路线，本技能退为方法补充。两者并用会撞车，按下表合并，不要并行跑两套：

| 冲突点 | 取哪个 | 为什么 |
|---|---|---|
| 路线编号 / 分层 | 项目专用技能 | 它按这个项目的真实结构分层，本技能六关是通用骨架 |
| 完成判据 | **两者取更严的** | 项目专用技能的判据通常针对该项目真实的危险，更严就该用它 |
| 进度回写位置 | 项目专用技能指定的地方 | 那是该项目其他 agent 也会读的位置；在本技能状态文件里留一行指向它 |
| 摸底诊断 / 深度选择 | 本技能 | 项目专用技能常缺这两块，这正是本技能要补的 |

映射方式：把六关按顺序**塞进**项目专用技能的层级，不要另起一套编号——学生同时看到两套"第 N 关 / 第 N 层"必然混乱。例如项目技能有 5 层，就把本技能的关卡 1–2 折进它的第 1 层，关卡 3–4 折进它的第 3–4 层，说清楚"你现在的位置在它的第几层"。

**项目内的 `README`、`AGENTS.md`、`docs/` 是事实权威。** 本技能只提供方法与路线，不复制事实——两处都写事实必然打架，且迟早有一处是错的。发现冲突时以项目内原文件为准。

**项目专用技能位于另一个检出目录时**（例如你学的是某个快照副本，而专用技能的文档指向主工程），先逐个核对它引用的文件在这个副本里是否真的存在。快照副本常常缺文件——**不存在就当场替换成副本里的真实路径并标注**，不要照着它给出的路径讲课，学生会找不到文件。

